import{readFile}from'node:fs/promises';import{contractCatalog}from'../src/contracts.mjs';for(const l of['en','ru'])for(const c of contractCatalog){const h=await readFile(`dist/${l}/${c.id}/index.html`,'utf8');if(!h.includes('SHAR Production')||!h.includes('https://sharprod.com/')||!h.includes('rel="canonical"')||!h.includes(l==='en'?'Verified example':'Проверенный пример'))throw new Error(`${l}/${c.id}`)}console.log('verified build: 10 contracts, 20 localized pages, positive and negative AJV results');
